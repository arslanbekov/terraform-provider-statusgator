---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "statusgator_ping_monitor Resource - statusgator"
subcategory: ""
description: |-
  Manages a StatusGator ping (ICMP) monitor.
---

# statusgator_ping_monitor (Resource)

Manages a StatusGator ping (ICMP) monitor.

## Example Usage

```terraform
# Ping monitor for server health
resource "statusgator_ping_monitor" "server1" {
  board_id       = "my-board-id"
  name           = "Server 1"
  host           = "server1.example.com"
  check_interval = 1
  regions        = ["us-east", "us-west"]
}

# Ping monitor in a group
resource "statusgator_ping_monitor" "database" {
  board_id       = "my-board-id"
  name           = "Database Server"
  host           = "db.internal.example.com"
  check_interval = 5
  group_id       = statusgator_monitor_group.infrastructure.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `board_id` (String) The ID of the board this monitor belongs to.
- `check_interval` (Number) Check interval in minutes.
- `host` (String) The host to ping.
- `name` (String) The name of the monitor.

### Optional

- `group_id` (String) The ID of the monitor group this monitor belongs to.
- `regions` (List of String) List of monitoring regions.

### Read-Only

- `id` (String) The unique identifier of the monitor.
- `paused` (Boolean) Whether the monitor is paused.
- `status` (String) Current status of the monitor (up, down, warn, unknown).
